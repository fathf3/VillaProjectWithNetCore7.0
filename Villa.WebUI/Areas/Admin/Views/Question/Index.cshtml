@model List<ResultQuestionDto>
@{
    ViewData["Title"] = "Question Listesi";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int counter = 0;
}



<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered table-responsive-sm">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Soru</th>
                            <th>Cevap</th>
                            <th>İşlemler</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            counter++;
                            <tr>
                                <th>@counter</th>
                                <th>@item.Title</th>
                                <td>@item.Answer</td>
                                
                                <td>
                                    <button onclick="deleteDeal(this)" controllerName="Question" id="@item.Id" class="m-1 badge badge-danger">Sil</button>
                                    <a asp-controller="Question" asp-action="Update" asp-route-id="@item.Id" class="m-1 badge badge-info">Güncelle</a>
                                </td>
                            </tr>
                        }

                        <div class="col-lg-3 mt-2">
                            <a asp-controller="Question" asp-action="Create" class="mb-3 btn btn-outline-dark">Yeni Question Ekle</a>
                        </div>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script src="~/admin-focus/js/deleteItem.js"></script>



