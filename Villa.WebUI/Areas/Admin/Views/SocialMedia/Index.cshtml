@model List<ResultSocialMediaDto>
@{
    ViewData["Title"] = "Sosyal Medya Listesi";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int counter = 0;
}



<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered table-responsive-sm">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Site Adı</th>
                            <th>Site Adresi</th>
                            <th>İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            counter++;
                            <tr>
                                <th>@counter</th>
                                <th>@item.Name</th>
                                <td>@item.Url</td>
                                
                                <td>
                                    <button onclick="deleteDeal(this)" controllerName="SocialMedia" id="@item.Id" class="m-1 badge badge-danger">Sil</button>
                                    <a asp-controller="SocialMedia" asp-action="Update" asp-route-id="@item.Id" class="m-1 badge badge-info">Güncelle</a>
                                </td>
                            </tr>
                        }

                        <div class="col-lg-3 mt-2">
                            <a asp-controller="SocialMedia" asp-action="Create" class="mb-3 btn btn-outline-dark">Yeni SocialMedia Ekle</a>
                        </div>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script src="~/admin-focus/js/deleteItem.js"></script>



